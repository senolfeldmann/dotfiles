# base
@c-development
@development-tools
git
htop
zsh
# clipboard access for tmux-yank: copying out of a terminal is wanted on
# every machine, not just desktops, so this stays in the base list
xsel
# script(1), used by install-brew.sh to wrap brew in a pseudo-TTY so
# brew's internal `sudo -k` does not kill the parent sudo cache
util-linux-script
# unzip is needed by install-nerd-fonts.sh but generally useful enough to
# stay in the base list. Preinstalled on Workstation/KDE spins; listed
# explicitly so apply.sh works on a barebones Fedora install too.
unzip

# libs and building
gcc
gcc-c++
gdb
lzma
glibc-devel
libstdc++-devel
openssl-devel
readline-devel
zlib-devel
libzstd-devel
libffi-devel
bzip2-devel
xz-devel
sqlite
sqlite-devel
sqlite-libs
libuuid-devel
gdbm-libs
perf
expat
expat-devel
mpdecimal
python3-pip
iperf3
# Ookla Speedtest CLI; repo comes from setup-fedora-repos.sh
speedtest

# containers
# Repo comes from setup-fedora-repos.sh (docker-ce.repo); group membership
# and daemon activation from tweaks/docker.sh.
docker-ce
docker-ce-cli
containerd.io
docker-buildx-plugin
docker-compose-plugin

# ai
glslang
glslc
ninja-build
vulkan-headers
vulkan-loader-devel
spirv-headers-devel
spirv-tools-devel

# mount.cifs for the SMB shares set up in tweaks/cifs-mounts.sh
cifs-utils

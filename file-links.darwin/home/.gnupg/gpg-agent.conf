# Touch ID while the MacBook is open; GPG-passphrase fallback while closed.
pinentry-program /Users/senol/.local/bin/pinentry-gpg

# Cache for 10 minutes since last use, but never longer than one hour.
default-cache-ttl 600
max-cache-ttl 3600
